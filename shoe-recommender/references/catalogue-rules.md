# Catalogue rules

Use only products stored in the local catalogue. A category or gender filter narrows results when that data exists; missing fields are uncertainty, not a match. Prefer products with a fresh, available listing and present their price decision beside the recommendation.
